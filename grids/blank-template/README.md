# blank-template

An intentionally-incomplete grid. Its cells hold commented prompts, no real content, so the engine correctly refuses to validate it: a blank grid is not a clone yet. CI skips this folder because of the `.template` marker.

Do not fill this folder in directly. Read it to learn what each cell wants, then follow `BUILD.md` to create your own grid with `scaffold`.
